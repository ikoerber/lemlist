# Lemlist API Configuration
#
# SETUP:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual values below
# 3. The .env file is automatically ignored by git (see .gitignore)
#
# USAGE:
# - If set here, these values will be pre-filled in the Streamlit UI
# - You can still override them in the UI if needed
# - Leave empty to use UI input only
#
# NOTE:
# - LEMLIST_API_KEY: Required for API access
# - CAMPAIGN_ID: Optional - you can use the campaign dropdown instead!

LEMLIST_API_KEY=a9b6f1f04c0b91d6e0cbe270057d2ca5
CAMPAIGN_ID=cam_Exq6AhZzywWwY7CPu

# HubSpot Integration (optional)
# Create a Private App in HubSpot: Settings → Integrations → Private Apps
# Required scopes: crm.objects.contacts.write
HUBSPOT_API_TOKEN=

# HubSpot App ID (only needed for Timeline Events, not for Notes or Property Sync)
# Get from: https://developers.hubspot.com/ → Your Apps → App ID
HUBSPOT_APP_ID=
